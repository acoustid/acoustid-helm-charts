apiVersion: v1
kind: Service
metadata:
  name: {{ include "acoustid-server.fullname" . }}
  labels:
    {{- include "acoustid-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "acoustid-server.selectorLabels" . | nindent 4 }}
