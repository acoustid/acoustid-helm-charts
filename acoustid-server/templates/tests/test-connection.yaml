apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acoustid-server.fullname" . }}-test-connection"
  labels:
    {{- include "acoustid-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "acoustid-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
